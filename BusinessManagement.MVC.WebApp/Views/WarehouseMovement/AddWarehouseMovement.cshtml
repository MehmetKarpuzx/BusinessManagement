@model BusinessManagement.MVC.DTO.WarehouseMovementDTOs.AddWarehouseMovementDto
@{
    ViewData["Title"] = "Depo Hareketi Ekle";
}

<div class="glass-card p-4">
    <div class="mb-4">
        <h2 class="fw-bold mb-0">
            <i class="fa-solid fa-plus text-primary me-2"></i>Yeni Depo Hareketi Ekle
        </h2>
        <p class="text-muted">Hareket detaylarını giriniz.</p>
    </div>

    <form asp-action="AddWarehouseMovement" method="post">
        <div class="row g-3">
            <div class="col-md-4">
                <label asp-for="ProcessTypeId" class="form-label fw-semibold">İşlem Tipi ID</label>
                <input asp-for="ProcessTypeId" type="number" class="form-control glass-input" required>
            </div>
            <div class="col-md-4">
                <label asp-for="SupplierId" class="form-label fw-semibold">Tedarikçi ID</label>
                <input asp-for="SupplierId" type="number" class="form-control glass-input">
            </div>
            <div class="col-md-4">
                <label asp-for="MaterialId" class="form-label fw-semibold">Hammadde ID</label>
                <input asp-for="MaterialId" type="number" class="form-control glass-input" required>
            </div>
            
            <div class="col-md-4">
                <label asp-for="Amount" class="form-label fw-semibold">Miktar</label>
                <input asp-for="Amount" type="number" class="form-control glass-input" required>
            </div>
            <div class="col-md-4">
                <label asp-for="UnitPrice" class="form-label fw-semibold">Birim Fiyat</label>
                <input asp-for="UnitPrice" type="number" class="form-control glass-input" required>
            </div>
            <div class="col-md-4">
                <label asp-for="TotalPrice" class="form-label fw-semibold">Toplam Tutar</label>
                <input asp-for="TotalPrice" type="number" class="form-control glass-input" required>
            </div>

            <div class="col-md-4">
                <label asp-for="InvoiceNo" class="form-label fw-semibold">Fatura No</label>
                <input asp-for="InvoiceNo" type="number" class="form-control glass-input">
            </div>
            <div class="col-md-4">
                <label asp-for="BranchId" class="form-label fw-semibold">Şube ID</label>
                <input asp-for="BranchId" type="number" class="form-control glass-input" required>
            </div>
            <div class="col-md-4">
                <label asp-for="TransactionDate" class="form-label fw-semibold">İşlem Tarihi</label>
                <input asp-for="TransactionDate" type="date" class="form-control glass-input" required value="@DateTime.Now.ToString("yyyy-MM-dd")">
            </div>

            <div class="col-md-12">
                <label asp-for="Description" class="form-label fw-semibold">Açıklama</label>
                <textarea asp-for="Description" class="form-control glass-input" rows="3"></textarea>
            </div>

            <div class="col-12 mt-4">
                <div class="d-flex gap-2 justify-content-end">
                    <a asp-action="Index" class="btn btn-outline-secondary px-4">İptal</a>
                    <button type="submit" class="btn btn-primary px-4">Kaydet</button>
                </div>
            </div>
        </div>
    </form>
</div>
